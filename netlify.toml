[build]
  base = "frontend"
  publish = "build"
  command = "npm install --legacy-peer-deps && npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  REACT_APP_API_URL = "https://hd-notes-backend.up.railway.app/api"