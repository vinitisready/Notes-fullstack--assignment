{
  "name": "hd-notes-app",
  "version": "1.0.0",
  "description": "Full-stack note-taking application with React TypeScript frontend and Node.js backend",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "test": "cd frontend && npm test"
  },
  "keywords": [
    "react",
    "typescript",
    "nodejs",
    "mongodb",
    "notes",
    "authentication"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
